{
  "name": "perq-deal-crawler",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "crawl": "curl -X POST http://localhost:8787/api/crawl"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
