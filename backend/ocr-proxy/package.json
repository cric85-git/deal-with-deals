{
  "name": "perq-ocr-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:set": "echo 'Run: wrangler secret put ANTHROPIC_API_KEY'"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
